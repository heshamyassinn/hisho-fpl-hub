# Hisho FPL Hub

Hisho FPL Hub v5.1 — iOS-ready Progressive Web App for Fantasy Premier League analytics, squad planning, fixtures, transfers, comparison, news, and mini-league intelligence.

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:4173`.

## Deploy

The repository includes `render.yaml` for Render deployment.
